# TypeScript Arc Starter Kit

Define your functions in `src/manifest.ts`. Export your Action name.

Build with `grunt`. It will run through eslint and Typescript checks, compile the code into the assets folder, and then upload to your application using mozusync as usual.
